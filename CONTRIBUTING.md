Issue も Pull Request も歓迎します。日本語で書くこと。

まず [README](README.md) のビルド手順を読んでおいてください。`third_party/mozc` の
クローンと `./scripts/build_mozc.sh` が済んでいないと、アプリはビルドできません。

## 特に気をつけること

普通の Android アプリと違う点は押さえておいてください。

### third_party を直接編集しない

`third_party/mozc` と `third_party/karukan` は上流の checkout で、gitignore されています。
そこを直接書き換えても他の人には届きません。

mozc / karukan 側に手を入れる必要があるときは `patches/` にパッチとして置いてください。
`scripts/build_mozc.sh` が適用します。作り方は、ツリーを編集してから

```bash
git -C third_party/mozc diff -- <触ったファイル> > patches/00NN-なにをするか.patch
```

出したら、素のツリーに順番どおり当たるか確認してください。

```bash
git -C third_party/mozc apply -R patches/00NN-....patch   # 戻して
git -C third_party/mozc apply --check patches/00NN-....patch  # 当たるか確認して
git -C third_party/mozc apply patches/00NN-....patch      # 戻す
```

同じファイルを複数のパッチが触る場合、後のパッチは前のパッチを適用した状態からの差分に
してください (`0003` が `0002` の上に乗っているのがその例です)。

### 他のフォークからコードを持ってこない

mozc には fork がいくつもあり、参考になる実装もあります。ただしコードをそのまま
持ち込むのは避けてください組みを読んで理解した上で、上流 mozc の API に対して
自分で書き直す形にしてください。ライセンスと由来がはっきりしなくなるためです。

参考にした実装がある場合は、PR の説明にどこを見たかを書いてもらえると助かります。

### ネットワークに出ない

このアプリの中心的な性質です。`AndroidManifest.xml` に `uses-permission` は 1 つも
ありません (AndroidX が注入する 1 件を除く)。

- 実行時に通信するコードを足さないでください
- 辞書やモデルの取得は `scripts/` のビルド時スクリプトで行い、成果物を同梱します
- `INTERNET` 権限を足す PR は、機能追加ではなく製品の性質の変更として扱います

### ユーザーが打った内容の扱い

学習履歴・ユーザー辞書は Android Keystore の鍵で暗号化して保存しています
(`patches/0002` と `SecureStore`)。ここを迂回する保存経路を足さないでください。

クリップボード履歴だけはあえてディスクに書いていませんスワードマネージャが
パスワードを置く場所であり、このキーボードで打たれたものですらないためです。
永続化したくなったときは、まずその判断から議論させてください。

パスワード欄では、mozc に本文を渡していません (`Context.input_field_type`)。

## 変える前に測る

このプロジェクトでは、変換や性能まわりの変更は推測ではなく実測断しています。
mozc はホストでもビルドできるので、小さな確認用プログラムを書いて `bazel run` で
回せます。実際、この方針で以下のような思い込みが潰れました。

- 「候補が遅いのは変換が重いから」→ 実際は JNI を渡る protobuf が 1 打鍵 78 KB で、
  その 98% がアプリ側で使っていないフィールドだった
- 「左文脈を渡せば誤変換が直る」→ 上流 mozc は日本語の左文脈を捨てていたので、
  渡すだけでは何も変わらなかった (むしろ悪化する例があった)

確認用プログラムは使い捨てで構いません。commit しないでください。ただし PR の説明には
測った結果を貼ってください速くなったはず」ではなく、前後の数字があると
レビューが一往復で済みます。

## テスト

```bash
./gradlew :app:testDebugUnitTest
```

ロジックが判断を含むなら、純関数に切り出してテストを付けてください。実際に切り出して
あるのは、候補の並び順 (`CandidateOrder`)、Enter キーの行き先 (`EnterRouting`)、
Backspace の振り分け (`BackspaceRouting`)、入力方式の解決 (`KeyboardStyle`) などです。

真理値表が小さいものは全パターン書いてください。Enter キーの分岐は 2 回続けて
順序を間違えており、どちらも「その組み合わせのテストが無かった」ために気づけませんでした。

`InputMethodService` そのもののテストは今のところありません。実機確認になります。

## 実機での確認

エミュレータは使っていません。ビルドした APK を渡して、作者が実機で確認しています。

PR を出すときは、どの入力で何を確認したかを書いてください。IME は「特定のアプリの
特定の入力欄でだけ壊れる」ことが多く、再現手順が無いと追えません。

## コードのスタイル

- コメントは「何をしているか」ではなくなぜそうなっているかいてください。
  とくに、そう書かないと壊れる理由や、素直な実装を選ばなかった理由。
- 周囲のコードに合わせてください。コメントの密度も含めて。
- 生成物 (`app/src/main/assets/layouts/*.json`、アイコン) は手で編集せず、
  `scripts/` の生成スクリプトを直して再生成してください。

## コミットと PR

コミットメッセージは日本語で。件名で何が変わるか分かれば十分です。

PR には次を書いてもらえると助かります。

- 何を直すか、なぜそれが問題か
- 測ったなら、その結果
- 実機で何を確認したか
- 参考にした実装があればその出典

## ライセンス

このリポジトリへの貢献は Apache License 2.0 の下で受け入れられます。
同梱物のライセンスは [NOTICE](NOTICE) を参照してください。

辞書やモデルなど、サイズの大きいものは、
そのままではマージできません。ビルド時の任意オプションにする形なら検討できます(karukanのモデルがそう)。
