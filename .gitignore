# Build output
/build/
**/build/
.gradle/
local.properties
.cxx/

# Vendored / generated
/tools/
/third_party/mozc/
!/patches/
/mozc/src/main/jniLibs/
/mozc/src/main/assets/mozc.data

# Release signing. The key can never be replaced once anything is published under it, so it lives
# outside the repository entirely and keystore.properties only points at it.
keystore.properties
*.jks
*.keystore

# IDE
.idea/
*.iml
/mozc/src/main/assets/dictionaries/

# Kotlin compiler session files
.kotlin/


# Built APKs handed over for testing
/dist/

# Dictionary sources for the system dictionary build
/third_party/dictionaries/
